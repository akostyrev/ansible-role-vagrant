---
molecule:
  test:
    sequence:
      - destroy
      - syntax
      - create
      - converge
      - idempotence

driver:
  name: docker

docker:
  containers:

    - name: ubuntu-vagrant
      image: ubuntu
      image_version: latest

    - name: fedora-vagrant
      image: fedora
      image_version: latest
      command: /bin/bash

verifier:
  name: testinfra
